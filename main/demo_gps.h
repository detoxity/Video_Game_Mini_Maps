// Drag-strip simulator (GPS_SOURCE_DEMO): stands still, launches at
// constant acceleration up Khreshchatyk, brakes, repeats. Exercises the
// map, the speed readout and the performance meter without hardware.
#pragma once

void demo_gps_start(void);
